import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render(<App/>,document.getElementById('root'))

//First parameter is what component we want to render
//and the second parameter is the element we want to hook